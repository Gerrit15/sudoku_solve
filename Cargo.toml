[package]
name = "sudoku_solve"
version = "0.1.0"
edition = "2021"
authors = ["Gerrit R"]

[dependencies]
csv = "1.3.0"
clap = {version = "4.4.8", features = ["derive"]}
rand = "0.8.5"
serde = {version = "1.0", features = ["derive"]}
serde_json = "1.0"

[dev-dependencies]
proptest = "1.0.0"
